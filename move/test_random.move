module lottery::test_random {
    use std::vector;
    use std::hash;
    
    public fun generate_random_numbers(): vector<u8> {
        // block::get_current_block_height()
        // block::get_block_hash()
        // timestamp::now_microseconds()
        
        vector::empty<u8>()
    }
}
